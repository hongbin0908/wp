package chapt6.u3

class Underscore {
  def main(arr:Array[String]) = {
    val arr = Array(1,2,3,4,5)
    println("Sum of all values in array is " + (0 /: arr){
      (sum,elem)=>sum+elem
    })
  }
}